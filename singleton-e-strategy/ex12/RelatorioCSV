public class RelatorioCSV implements Relatorio {
    @Override
    public void gerarRelatorio(String conteudo) {
        System.out.printf("Gerando relatório em CSV: %s\n", conteudo);
    }

    @Override
    public String getFormato() {
        return "CSV";
    }
}
